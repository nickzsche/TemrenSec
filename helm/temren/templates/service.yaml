apiVersion: v1
kind: Service
metadata:
  name: {{ include "temren.fullname" . }}-api
  labels:
    {{- include "temren.labels" . | nindent 4 }}
    app.kubernetes.io/component: api
spec:
  type: {{ .Values.api.service.type }}
  ports:
    - name: http
      port: {{ .Values.api.service.port }}
      targetPort: http
      protocol: TCP
  selector:
    {{- include "temren.selectorLabels" (dict "root" . "component" "api") | nindent 4 }}
