apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: temren
resources:
  - k8s.yaml
# Override the placeholder secrets in an overlay, e.g.:
#   secretGenerator:
#     - name: temren-secrets
#       behavior: replace
#       envs: [secrets.env]
